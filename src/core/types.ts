/**
 * core/types.ts
 * Temel branded tip re-export'ları — runtime/language-services katmanı için.
 * Canonical kaynak: src/types/core.ts
 */

export type { UUID, Timestamp, MetaRecord } from "../types/core";
