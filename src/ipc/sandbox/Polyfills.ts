/** Stub — Phase 2: console.log → onOutput köprüsü */
export function applyPolyfills(_sandbox: unknown, _opts: unknown): () => void {
  // TODO: QuickJS context'e polyfill'leri enjekte et
  return () => { /* cleanup */ };
}
